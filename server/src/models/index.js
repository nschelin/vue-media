const { Genre } = require('./genre');
const { MediaType } = require('./mediaType');
const { Album } = require('./album');
const { Artist } = require('./artist');
module.exports = { Genre, MediaType, Album, Artist };
